# SpendingMonitor

`SpendingMonitor` applies spending controls inside the SDK runtime.

## Typical Checks

- daily limit
- total limit
- balance and allowance awareness

## Why It Exists

Autonomous agents need programmatic limits. The monitor prevents the agent from treating a wallet like an unbounded credit line.
